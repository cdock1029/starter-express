diff --git a/node_modules/@remix-run/core/assetServer.js b/node_modules/@remix-run/core/assetServer.js
index 05a7e49..2c84dc5 100644
--- a/node_modules/@remix-run/core/assetServer.js
+++ b/node_modules/@remix-run/core/assetServer.js
@@ -91,7 +91,7 @@ function createRequestHandler(config, {
 
   let app = express__default['default']();
   app.disable("x-powered-by");
-  app.use(morgan__default['default']("dev"));
+  // app.use(morgan__default['default']("dev"));
   app.use(cors__default['default']());
   app.get("*", handleRequest);
   return app;
diff --git a/node_modules/@remix-run/core/rollup/watchInput.js b/node_modules/@remix-run/core/rollup/watchInput.js
index 6cd0a6f..8995fb0 100644
--- a/node_modules/@remix-run/core/rollup/watchInput.js
+++ b/node_modules/@remix-run/core/rollup/watchInput.js
@@ -2,14 +2,14 @@
 
 Object.defineProperty(exports, '__esModule', { value: true });
 
-var fs = require('fs');
-var chokidar = require('chokidar');
-var tmp = require('tmp');
+// var fs = require('fs');
+// var chokidar = require('chokidar');
+// var tmp = require('tmp');
 
 function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }
 
-var chokidar__default = /*#__PURE__*/_interopDefaultLegacy(chokidar);
-var tmp__default = /*#__PURE__*/_interopDefaultLegacy(tmp);
+// var chokidar__default = /*#__PURE__*/_interopDefaultLegacy(chokidar);
+// var tmp__default = /*#__PURE__*/_interopDefaultLegacy(tmp);
 
 /**
  * Enables setting the compiler's input dynamically via a hook function.
@@ -19,6 +19,7 @@ function watchInput({
   sourceDir,
   getInput
 }) {
+  /*
   let tmpfile = tmp__default['default'].fileSync();
   let startedWatcher = false;
 
@@ -40,6 +41,7 @@ function watchInput({
   function handleAdd() {
     triggerRebuild();
   }
+  */
 
   return {
     name: "watch-input",
@@ -58,12 +60,14 @@ function watchInput({
       // This is a workaround for a bug in Rollup where this.addWatchFile does
       // not correctly listen for files that are added to a directory.
       // See https://github.com/rollup/rollup/issues/3704
+      /*
       if (!startedWatcher) {
         await startWatcher();
         startedWatcher = true;
       }
 
       this.addWatchFile(tmpfile.name);
+      */
     }
 
   };
